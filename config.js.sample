var config = {};

config.port = 4000;
config.sessionSecret = 'not secret';

config.db = {
    development: {
        host: 'localhost',
        database: 'attendance',
        username: 'root',
        password: null,
        logging: console.log
    },
    production: {
        host: 'localhost',
        database: 'attendance',
        username: 'root',
        password: null,
        logging: false
    },
    test: {
        host: 'localhost',
        database: 'attendance_test',
        username: 'root',
        password: null,
        logging: false
    },
};

// Session cookie generated when logging into my.cs.illinois.edu
// Used to fetch i-card photos
config.myCSSessionCookie = 'key=value';

module.exports = config;
